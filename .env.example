# PostgreSQL Configuration
POSTGRES_USER=restosmart
POSTGRES_PASSWORD=restosmart_password_change_me
POSTGRES_DB=restosmart
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=5000
NODE_ENV=production

# Database URL (auto-generated from above values)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# Session Secret (generate a secure random string for production)
SESSION_SECRET=change_this_to_a_secure_random_string_in_production
